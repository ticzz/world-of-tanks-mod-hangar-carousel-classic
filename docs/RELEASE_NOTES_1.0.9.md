# Hangar Carousel Classic 1.0.9

- Separate master switches for filtering, sorting, and vehicle-card statistics.
- Keep filtering active when XVM-compatible sorting is disabled.
- Combine multiple active filters with AND logic, such as premium vehicles with crew below 75%.
- Clarify that HCC panel sorting buttons select one rule at a time; multiple hierarchical rules are configured through the ModsSettingsAPI sorting field.
- Add MSA tooltips explaining filter, sorting, card-statistics, carousel-row, and action-card settings.
- Remove duplicate configuration and runtime loading function definitions.
- Improve full-package build validation when `-PackagePath` is missing.
